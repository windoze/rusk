# `examples/lsp`

This directory is a minimal multi-file Rusk program intended for testing editor tooling such as
`rusk-lsp`.

Run it with the CLI:

```sh
cargo run --bin rusk -- examples/lsp/main.rusk
```

