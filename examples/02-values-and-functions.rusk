// 02 - 值与函数
//
// 这个示例覆盖：
// - `let` 绑定与（可选的）类型标注
// - 函数定义 `fn ...`、参数/返回类型
// - `main` 的返回值（`rusk` CLI 会在返回值非 `unit` 时把它打印出来，方便调试）
//
// 对比（TypeScript）：
//   function add(a: number, b: number): number { return a + b }

fn add(a: int, b: int) -> int { a + b }

fn main() -> int {
    let a = 40;
    let b = 2;
    let c = add(a, b);
    std::println(f"{a} + {b} = {c}");
    c
}
