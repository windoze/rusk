// `std` is a source wrapper around host modules.
//
// It is only loaded by the compiler when the `_std_host` host module is declared.

pub use crate::_std_host::print;
pub use crate::_std_host::println;

