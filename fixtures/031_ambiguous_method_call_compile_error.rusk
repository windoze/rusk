// expect: compile_error ambiguous method

struct S { x: int }

trait A { fn foo() -> int; }
trait B { fn foo() -> int; }

impl A for S { fn foo() -> int { self.x + 1 } }
impl B for S { fn foo() -> int { self.x + 2 } }

fn main() -> int {
    let s = S { x: 0 };
    s.foo()
}
