// expect: compile_error cannot call mutable method

struct Counter { n: int }

impl Counter {
    fn inc() {
        self.n = self.n + 1;
        ()
    }
}

fn main() -> int {
    readonly c = Counter { n: 0 };
    c.inc();
    0
}

