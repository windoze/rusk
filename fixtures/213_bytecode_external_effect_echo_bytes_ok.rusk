// expect: ok bool true

interface TestFfi { fn echo_bytes(b: bytes) -> bytes; }

mod test {
    pub extern fn bytes_eq(a: bytes, b: bytes) -> bool;
}

fn main() -> bool {
    test::bytes_eq(@TestFfi.echo_bytes(b"hi"), b"hi")
}
