// expect: ok int 431

fn main() -> int {
    let total = 0;

    for c in "é" {
        total = total + c.to_int();
    };

    readonly bs = b"ABC";
    for b in bs {
        total = total + b.to_int();
    };

    total
}
