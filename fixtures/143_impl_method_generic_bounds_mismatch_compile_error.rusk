// expect: compile_error bounds do not match interface

trait Show { fn show() -> string; }
trait Hash { fn hash() -> int; }

interface Pair<T> {
    fn pair<U: Show>(u: U) -> (T, U);
}

struct Box<T> { v: T }
struct S { x: int }

impl Show for S { fn show() -> string { "S" } }
impl Hash for S { fn hash() -> int { self.x } }

impl<T> Pair<T> for Box<T> {
    fn pair<U: Hash>(u: U) -> (T, U) { (self.v, u) }
}

fn main() -> int { 0 }
